<% is_new_question = @question.new_record? %>
<%= form_with model: is_new_question ? [@test, @question] : @question, local: true do |form| %>
  <%= form.label :body %>
  <%= form.text_field :body %>
  <%= form.submit is_new_question ? 'Создать' : 'Обновить' %>
<% end %>
