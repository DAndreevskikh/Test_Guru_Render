<tr>
  <td><%= question_counter + 1 %></td>
  <td><%= question.body %></td>
  <td>
    <%= link_to t('show'), admin_question_path(question) %> |
    <%= link_to t('edit'), edit_admin_question_path(question) %> |
    <%= link_to t('delete'), admin_question_path(question), method: :delete, data: { confirm: t('confirmations.are_you_sure') } %>
  </td>
</tr>
